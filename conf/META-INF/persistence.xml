<persistence xmlns="http://xmlns.jcp.org/xml/ns/persistence"
			 xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
			 xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence http://xmlns.jcp.org/xml/ns/persistence/persistence_2_1.xsd"
			 version="2.1">

	<persistence-unit name="defaultPersistenceUnit" transaction-type="RESOURCE_LOCAL">
		<provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>
		<non-jta-data-source>DefaultDS</non-jta-data-source>
		<properties>
			<property name="hibernate.dialect" value="org.hibernate.dialect.PostgreSQL95Dialect"/>
			<!-- Erzeugt die Datenbank (todo: nur zum Entwicklen, später entfernen!) -->
			<!-- Variante: behält Inhalt, macht jedoch nur neue Tabellen -->
			<!--<property name="hibernate.hbm2ddl.auto" value="create-only"/>-->
			<!-- Variante: löscht Inhalt bei jedem Compile, erstellt jedoch immer alle Tabellen (korrekt) neu -->
			<property name="hibernate.hbm2ddl.auto" value="create"/>
		</properties>
	</persistence-unit>

	<persistence-unit name="testPersistenceUnit" transaction-type="RESOURCE_LOCAL">
		<provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>
		<non-jta-data-source>DefaultDS</non-jta-data-source>
		<properties>
			<property name="hibernate.dialect" value="org.hibernate.dialect.PostgreSQL95Dialect"/>
			<!-- Erzeugt die Datenbank -->
			<!-- Variante: löscht Inhalt bei jedem Compile, erstellt jedoch immer alle Tabellen (korrekt) neu -->
			<property name="hibernate.hbm2ddl.auto" value="create-only"/>
		</properties>
	</persistence-unit>

</persistence>
